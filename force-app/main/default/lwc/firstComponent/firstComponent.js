import { LightningElement } from 'lwc';

export default class FirstComponent extends LightningElement {
    

    titleMessage = 'Learing LWC!!!'; // property(reactive)

    bodyMessage = 'Data Binding - Its a way to bring values from JS to HTML !!!';
}